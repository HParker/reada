<div class="container">
  <table class="table-minimal">
    <tbody>
      <tr>
        <th>Name</th>
        <th>Shows</th>
        <th>Status</th>
      </tr>
      <% @feeds.each do |feed| %>
        <% cache feed do %>
          <tr>
            <td><%= link_to feed.title, feed %></td>
            <td><%= feed.stories.count %></td>
            <td><span class="badge-<%= feed.status %>"><%= feed.status%></span></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
